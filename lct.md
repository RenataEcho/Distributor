实现要点

分层渲染：先画 flow-edges，再画 flow-nodes。连线层在下，避免折线横穿线框。
正交折线：只用水平 / 垂直 line 或 polyline。分叉与汇聚走节点间隙的专用通道，禁止斜线穿框。
端点落边：折线起止落在节点顶 / 底 / 左 / 右边，悬空端点视为断流。
闭环：驳回、失败、重提必须接到起点顶边。回环走外侧通道，禁止贴画布边缘停住（看起来像断线）。
间距：相邻节点纵向 ≥24px；菱形与上下节点 ≥30px；左右分支框与菱形顶点 ≥16px。
一菱形一问题：菱形只问一个是 / 否。判定公式、口径、比较符写在菱形内或紧邻副文案，禁止把「收益达标」合成一步。
文本：主文案 11–13px；副文案 10px + muted。超 12 字用 tspan 换行，线框留足内边距。
分支标注：「是 / 通过」贴对应折线旁用绿色；「否 / 驳回」用红色。
多图用横向 Tab：.flow-tab-bar 切流程 1 / 2 / 3，单面板只展示一张，禁止纵向堆叠。图例放面板底部共用。
画布可滚：.flow-svg-wrap { overflow:auto; max-height:72vh }。svg 写死 width + viewBox，窄屏横向滚，不重排。
箭头 id 隔离：每张 SVG 独立 marker id（如 flow-arrow-1），同页复用同一 id 会串箭头。
虚线旁路：可选、汇总、异步旁路用 stroke-dasharray="4 3"。
深链：?tab=flow；多图再加 &flow=1|2|3。
规则抽屉只指向：写「见页内业务流程 Tab」，不在抽屉里复述文字版全流程，也不嵌第二份图。

硬限制（做不到 / 不要做）

手写绝对坐标，无布局引擎。移动或加高一个节点，必须重算所有相连折线与通道；改一处容易漏改边。
无自动避让。折线不会绕开节点，线穿框是最高频事故。先定节点框，再对端点。
菱形是写死 polygon。改文案不能只改 height，必须重算四个顶点，否则字溢出。
SVG 文本不自动折行。超长必须手写 tspan，不会像 HTML 那样 wrap。
viewBox 写死。加节点必须同步加大 width / height / viewBox，否则节点被裁切。
不做响应式重排。窄屏只横向滚动，不会像 Grid 那样回流节点。
静态图，不交互。节点不能点、不能高亮当前态、不跟 Demo 表数据联动。
复杂度上限。单图超过约 12 个节点或 3 层分叉，拆成流程 1 / 2 / 3。硬堆会导致通道打架、回环贴边。
不表达并发与时序。并行、等待、超时只能靠橙色节点或虚线示意，没有泳道 / 甘特。
色值写死 hex。未接 CSS 变量，也没有暗色主题适配。
禁用 Mermaid / 流程图库。颜色、正交通道、分层渲染、图例对不齐，视觉不可控。
不进抽屉、不进规则正文。第二份图会和页内 Tab 漂移。
不接真实状态机。图是研发说明，不驱动后台表状态，也不做权限 / 持久